{
  "builds": [
    { "src": "public/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/frontend/(.*)", "dest": "/public/frontend/$1" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
